interface:
  display_name: "CSV Upload Encoding"
  short_description: "Fix garbled CSV upload text safely"
  default_prompt: "Use $csv-upload-encoding to fix garbled Chinese text when uploading CSV files."
